{
  "name": "outl-word-count",
  "version": "1.0.0",
  "description": "Example outl plugin: toast when a block crosses a word-count milestone.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=iife --platform=neutral --target=es2022 --outfile=index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@outl/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
