[package]
name = "outl-shortcuts"
description = "Shared keyboard binding catalog (Action / Chord / Mode) consumed by outl-tui and outl-desktop, plus the Action/Capability parity catalog consumed by all three clients including outl-mobile."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
publish = false

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
